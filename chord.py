import random
import notes

notes = notes.Notes()

print(notes.get_scale('D', 'min'))
print(notes.get_scale('C', 'maj'))
print(notes.get_scale('F', 'min_pent'))
print(notes.get_scale('A', 'min_pent'))

exit()